# 附錄 C — STM32N6 X-CUBE-AI Static Analysis (proxy)

> **重要聲明**:本附錄是用 Python `onnx` library 解析 `models/lstm_rul.onnx`
> 的 graph 後,**參照 ST 公開資料**(AN5354 / RM0498 / X-CUBE-AI 9.x release
> notes)做的**靜態分析估算**。**不是 X-CUBE-AI 工具實際跑出來的 trace**。
>
> 真正的 X-CUBE-AI 9.x 報告需要 ST 帳號 + Windows GUI 工具,本團隊計畫於
> W3 完成實機 trace 後**取代本附錄**。在那之前本表給出 order-of-magnitude
> 上界,**所有 latency / memory 數字應視為 ±2× 不確定性**。

## C.1 模型摘要

- ONNX 檔: `models\lstm_rul.onnx`
- 參數總數: 54,093
- Weight FLASH(FP32 export): 211.3 KB
- Weight FLASH(INT8 量化估): **52.8 KB**
- Activation peak SRAM(INT8): **32.0 KB**
- ONNX nodes: 52
- Initialisers: 22

## C.2 STM32N6 配適

STM32N6 Neural-ART NPU spec(RM0498):

- **300 GOPS @ 1 GHz**(INT8 MAC throughput,peak)
- **1 MB ML SRAM**(activation buffer)
- **1.6 MB ML FLASH**(weight storage)

| 資源 | 模型需求 | NPU 容量 | 配適? |
|---|---:|---:|:---:|
| Weight FLASH | 52.8 KB | 1638.4 KB | ✅ |
| Activation SRAM | 32.0 KB | 1024 KB | ✅ |

## C.3 Op dispatch breakdown

依 X-CUBE-AI 9.x 公開 op support matrix(AN5354):

| Op | 數量 | NPU? |
|---|---:|:---:|
| `Concat` | 6 | ✅ NPU |
| `Expand` | 1 | ❌ CPU fallback |
| `Gather` | 1 | 🟡 NPU (部分) |
| `Gemm` | 2 | ✅ NPU |
| `LSTM` | 2 | 🟡 NPU (部分) |
| `Mul` | 2 | ✅ NPU |
| `Relu` | 1 | ✅ NPU |
| `Reshape` | 4 | ✅ NPU |
| `Shape` | 3 | ❌ CPU fallback |
| `Slice` | 22 | ✅ NPU |
| `Squeeze` | 1 | ✅ NPU |
| `Transpose` | 4 | ✅ NPU |
| `Unsqueeze` | 3 | ✅ NPU |

**Dispatch 統計**:
- NPU 完全加速: 45 個 op
- NPU 部分(LUT 近似 sigmoid/tanh): 3 個 op
- CPU fallback: 4 個 op
- Graph optimisation 移除: 0 個 op

## C.4 Latency 估算

假設(保守):

- NPU 有效利用率 = **40 %**
  (peak 300 GOPS,實際因 memory bandwidth 落到 ~ 40 %,ST AN5354 §Performance)
- CPU fallback 跑在 Cortex-M55 ~ 50 MOPS(無 DSP 加速)
- 單樣本 inference,不考慮 batching

| 量 | 估值 |
|---|---:|
| 總 MAC | 3,281,954 |
| NPU MAC | 3,281,954 |
| CPU MAC | 0 |
| **NPU latency 估** | **54.7 µs** |
| CPU fallback latency | 0.0 µs |
| **總 latency 估** | **54.7 µs** |

**對比 v2.1 §1.4 承諾**:STM32N6 NPU LSTM 0.3 ms = 300 µs。本估算結果
在 54.7 µs **符合** 此承諾,
但有 ±2× 不確定區間,**真實數字可能 27–109 µs**。

## C.5 Per-node breakdown(top 10 by MAC)

| Node | Op | Dispatch | MACs | Weights (INT8 KB) |
|---|---|:---:|---:|---:|
| `node_LSTM_67` | `LSTM` | 🟡 | 3,244,032 | 2.2 |
| `node_LSTM_128` | `LSTM` | 🟡 | 32,768 | 0.5 |
| `node_linear` | `Gemm` | ✅ | 4,096 | 2.0 |
| `node_linear_1` | `Gemm` | ✅ | 1,024 | 0.0 |
| `node_relu` | `Relu` | ✅ | 32 | 0.0 |
| `node_Mul_77` | `Mul` | ✅ | 1 | 0.0 |
| `node_Mul_135` | `Mul` | ✅ | 1 | 0.0 |
| `node_Shape_0` | `Shape` | ❌ | 0 | 0.0 |
| `node_Concat_7` | `Concat` | ✅ | 0 | 0.0 |
| `node_zeros` | `Expand` | ❌ | 0 | 0.0 |

## C.6 W3 待補的真實 X-CUBE-AI trace

以下項目本附錄**無法**提供,需在 W3 用 ST 工具補:

1. **實際 INT8 量化精度損失**(本估算假設 quantisation 完全無損)
2. **實際 NPU utilisation**(本估算用 40 % heuristic;ST 工具會給每層真實值)
3. **每層 cycle-accurate latency**(本估算只給總體 order)
4. **Buffer placement**(activation 是否真的 fit ML SRAM,還是要 spill 到外部 PSRAM)
5. **Power consumption**(NPU active vs CPU fallback 功耗差約 5×)

一旦 W3 在 X-CUBE-AI 9.x 跑完,本附錄會被替換為實機 trace 截圖 + ST 報告。

---

_Generated by `scripts/onnx_static_analysis.py` from `models\lstm_rul.onnx`._